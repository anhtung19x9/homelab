elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
#      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.19ninety-three.com"
#      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.19ninety-three.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.19ninety-three.com
      server_name: 19ninety-three.com

dendrite:
  dendrite_config:
    global:
      server_name: matrix.19ninety-three.com
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.19ninety-three.com
    tls:
      - hosts:
          - matrix.19ninety-three.com
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
